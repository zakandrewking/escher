define(["builder/Builder", "builder/Viewer", "builder/KeyManager", "vis/data-menu"],
       function(bu, vi, km, dm) {
           return { Builder: bu,
		    Viewer: vi,
		    KeyManager: km,
		    DataMenu: dm };
       });

