var BuildInput = require('../BuildInput')

const describe = require('mocha').describe
const it = require('mocha').it

// Tests for BuildInput are not yet written
describe('BuildInput', () => {
  it.skip('pending', () => {})
})
